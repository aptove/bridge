# @aptove/bridge-linux-arm64

Platform-specific binary package for Linux arm64. This package is automatically installed as an optional dependency of [@aptove/bridge](https://www.npmjs.com/package/@aptove/bridge) and should not be installed directly.

## Installation

```bash
npm install -g @aptove/bridge
```

See the [main package](https://www.npmjs.com/package/@aptove/bridge) for full documentation.
